{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Time Tracker</h1>
    <p>What would you like to start with?</p>
    <ul>
        {% if is_granted('ROLE_ADMIN') %}<li> <a href="{{ path('user_index') }}">Create Employee record</a>  </li>{% endif %}
            {% if is_granted('ROLE_ADMIN') %<li><a href="{{ path('rate_index') }}">Create rate</a> </li> {% endif %}
            {% if is_granted('ROLE_ADMIN') %}<li><a href="{{ path('working_time_index') }}">View working time</a></li> {% endif %}
            {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('working_time_index') }}">Log working time</a></li>
             {%  endif %}
    </ul>

</div>
{% endblock %}
