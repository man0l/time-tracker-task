{% extends 'base.html.twig' %}

{% block title %}Edit Rate{% endblock %}

{% block body %}
    <h1>Edit Rate</h1>

    {{ include('rate/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    <a href="{{ path('rate_index') }}">back to list</a>
    <br> <br>
    {{ include('rate/_delete_form.html.twig') }}
{% endblock %}
